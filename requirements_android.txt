flet==0.28.3
Pillow
numpy
ai-edge-litert; sys_platform == 'linux'
# Notes: 
# - ai-edge-litert is the modern replacement for tflite-runtime.
# - We include it conditionally for Linux (Android build env) to avoid issues on Windows dev machine if not available.
